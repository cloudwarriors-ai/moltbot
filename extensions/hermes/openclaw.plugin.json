{
  "id": "hermes",
  "uiHints": {
    "baseUrl": {
      "label": "Hermes Server URL",
      "placeholder": "http://localhost:3345",
      "help": "Base URL of the Hermes orchestration server (or use ${HERMES_BASE_URL})"
    },
    "apiKey": {
      "label": "API Key",
      "sensitive": true,
      "placeholder": "your-api-key",
      "help": "API key for Hermes authentication (or use ${HERMES_API_KEY})"
    },
    "organizationId": {
      "label": "Organization ID",
      "advanced": true,
      "placeholder": "org-...",
      "help": "Organization ID for multi-tenant filtering (or use ${HERMES_ORG_ID})"
    },
    "timeoutMs": {
      "label": "Request Timeout (ms)",
      "advanced": true,
      "placeholder": "30000",
      "help": "Default request timeout in milliseconds"
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "baseUrl": {
        "type": "string"
      },
      "apiKey": {
        "type": "string"
      },
      "organizationId": {
        "type": "string"
      },
      "timeoutMs": {
        "type": "number"
      }
    }
  }
}
