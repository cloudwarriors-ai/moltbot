{
  "name": "@openclaw/slm-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server/main.ts",
    "build": "rm -rf dist && tsc -p tsconfig.json && node scripts/copy-client.mjs",
    "start": "node dist/server/main.js",
    "test": "vitest run --config vitest.config.ts",
    "hash-password": "node scripts/hash-password.mjs"
  },
  "dependencies": {
    "express": "^5.2.1",
    "ws": "^8.19.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.2.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
